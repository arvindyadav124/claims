# Leave empty in development to use the Vite proxy (`vite.config.ts` forwards `/api` → Django).
# For production or non-proxied dev, set the full API origin (no trailing slash), e.g.:
# VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=
